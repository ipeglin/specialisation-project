# Windows 11 Conda Environment for TCP Preprocessing Pipeline
#
# Prerequisites:
#   Install scoop (https://scoop.sh):
#     Set-ExecutionPolicy RemoteSigned -Scope CurrentUser
#     irm get.scoop.sh | iex
#
#   Install Windows-specific tools via scoop:
#     scoop install git-annex 7zip
#
# Setup:
#   conda env create -f environment-windows.yml
#   conda activate masters_thesis
#
# Note: scipy is excluded as it had issues on Windows conda.
# If needed, install via pip: pip install scipy

name: masters_thesis
channels:
  - conda-forge
  - defaults
dependencies:
  # Python
  - python=3.11
  - pip

  # Core scientific computing (directly used)
  - numpy
  - pandas
  - scipy # Install via pip if conda fails: pip install scipy
  - matplotlib
  - seaborn
  - h5py

  # Neuroimaging (directly used)
  - nilearn
  - nibabel

  # Data processing utilities
  - tqdm
  - joblib

  # Jupyter/IPython (for notebooks and development)
  - ipython
  - ipykernel
  - jupyter_core
  - jupyter_client

  # DataLad and git-annex (for dataset management)
  - datalad
  # - git-annex  # Not available via conda on Windows - install via scoop instead
  - git

  # File compression (for DataLad)
  # - p7zip  # Not available via conda on Windows - 7zip installed via scoop instead
  - bzip2

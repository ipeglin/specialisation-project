---
phase: 01.1-participants-txt-subject-filter
verified: 2026-03-21T10:45:00Z
status: passed
score: 4/4 must-haves verified
re_verification: false
---

# Phase 01.1: Participants TXT Subject Filter — Verification Report

**Phase Goal:** Add optional --participants-file CLI flag to both parcellation entry points that filters discovered fmriprep subjects against a user-specified subject list, with hard error on missing subjects.
**Verified:** 2026-03-21T10:45:00Z
**Status:** PASSED
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | When --participants-file is provided, only subjects listed in the file AND discovered by fmriprep are parcellated | VERIFIED | `apply_participants_filter()` computes `[s for s in participants_subjects if s in discovered_set]`, preserving participants file order; both entry points call this before their subject iteration loops |
| 2 | When --participants-file is omitted, all discovered fmriprep subjects are parcellated (unchanged behaviour) | VERIFIED | Both entry points guard filter application with `if self.participants_file is not None:` / `if args.participants_file is not None:`, with `default=None` on the argparse argument |
| 3 | If any subject in participants.txt is NOT found in fmriprep output, the pipeline exits with an error listing ALL missing subjects | VERIFIED | `apply_participants_filter()` collects ALL missing subjects into a list before raising `ValueError` with a formatted list; no early-exit on first missing |
| 4 | Subjects discovered by fmriprep but NOT in participants.txt are silently excluded with a log message | VERIFIED | `excluded_count = len(discovered_set) - len(filtered)` then `print(f"{WARNING} {excluded_count} fmriprep subject(s) excluded by participants filter")` when count > 0 |

**Score:** 4/4 truths verified

---

### Required Artifacts

| Artifact | Expected | Exists | Substantive | Wired | Status |
|----------|----------|--------|-------------|-------|--------|
| `tcp/preprocessing/utils/participants_filter.py` | Standalone participants file parsing and intersection logic exporting `load_participants_file`, `apply_participants_filter` | YES | YES — 119 lines, both public functions fully implemented with comment parsing, sub- normalisation, deduplication, FileNotFoundError, ValueError with full missing-subject list | YES — imported by both parcellation files | VERIFIED |
| `tcp/preprocessing/parcellate_hcp_subjects.py` | --participants-file CLI flag on HCPParcellationRunner entry point | YES | YES — argparse flag at line 243, `self.participants_file` attribute at line 47, filter call at lines 119-124, runner wiring at line 286 | YES — filter applied in `parcellate_hcp_subjects()` before the empty-list guard | VERIFIED |
| `tcp/preprocessing/hcp_parcellation.py` | --participants-file CLI flag on multi-subject CLI path | YES | YES — argparse flag at line 458, filter applied in `else` (multi-subject) branch only at lines 485-490, single-subject path intentionally unchanged | YES — `parcellate_subjects_parallel()` receives filtered `subject_ids` variable | VERIFIED |

---

### Key Link Verification

| From | To | Via | Status | Detail |
|------|----|-----|--------|--------|
| `tcp/preprocessing/parcellate_hcp_subjects.py` | `tcp/preprocessing/utils/participants_filter.py` | `from tcp.preprocessing.utils.participants_filter import load_participants_file, apply_participants_filter` | WIRED | Import at line 37; both functions called in `parcellate_hcp_subjects()` at lines 120-123 |
| `tcp/preprocessing/hcp_parcellation.py` | `tcp/preprocessing/utils/participants_filter.py` | `from tcp.preprocessing.utils.participants_filter import load_participants_file, apply_participants_filter` | WIRED | Import at line 36; both functions called in multi-subject `else` branch at lines 486-489 |
| `tcp/preprocessing/utils/participants_filter.py` | filter call site | `apply_participants_filter` receives discovered subjects list | WIRED | In `parcellate_hcp_subjects.py`, `hcp_subjects_to_parcellate` (assembled from subject_file_mapping) is passed as `discovered_subjects`; in `hcp_parcellation.py`, `args.subject_ids` is passed |

---

### Requirements Coverage

No requirement IDs were declared for this phase (`requirements: []` in PLAN frontmatter). Phase is not mapped to REQUIREMENTS.md entries.

---

### Anti-Patterns Found

| File | Lines | Pattern | Severity | Impact |
|------|-------|---------|----------|--------|
| `tcp/preprocessing/hcp_parcellation.py` | 65-66, 84, 200 | Cerebellar atlas TODO and zeros placeholder | Info | Pre-existing, unrelated to participants filter feature; cerebellar atlas stub exists in `HCPParcellator` class, not in the `--participants-file` addition |

No anti-patterns introduced by this phase. The cerebellar placeholder was present before phase 01.1 and is unrelated to the filtering feature.

---

### Human Verification Required

None. All observable truths can be verified programmatically. The import check (`from tcp.preprocessing.utils.participants_filter import load_participants_file, apply_participants_filter` — exits 0 under `conda run -n masters_thesis`) confirms the module is importable and the logic is substantively implemented.

---

### Commits Verified

All three commits documented in SUMMARY confirmed present in git log:

- `2d4f4c0` — feat(01.1-01): add participants_filter.py utility module
- `fb8f72a` — feat(01.1-01): wire --participants-file into parcellate_hcp_subjects.py
- `62060bc` — feat(01.1-01): wire --participants-file into hcp_parcellation.py multi-subject path

---

### Summary

Phase 01.1 goal is fully achieved. The three-file implementation delivers exactly what was specified:

1. `participants_filter.py` is a substantive standalone utility — not a stub. It handles all specified parsing edge cases (comment lines, inline comments, empty lines, sub- normalisation, deduplication) and raises `ValueError` listing ALL missing subjects before stopping.

2. `parcellate_hcp_subjects.py` integrates the filter at the correct insertion point (after assembling `hcp_subjects_to_parcellate`, before the empty-list guard), so the existing "No HCP subjects need parcellation" path still fires correctly when the filter produces an empty result.

3. `hcp_parcellation.py` applies the filter only to the multi-subject `else` branch. The single-subject path (`if args.subject_id:`) is intentionally unchanged, matching the plan decision.

4. Both entry points use `default=None` on the argparse argument, so omitting `--participants-file` preserves the previous behaviour with zero code-path difference.

No gaps found.

---

_Verified: 2026-03-21T10:45:00Z_
_Verifier: Claude (gsd-verifier)_
